<div class="mx-auto md:w-2/3 w-full pt-24">
  <h1 class="font-bold text-4xl">New field</h1>

  <%= render 'form', field: @field, venue: @venue, cities: @cities %>

  <%= link_to "Back to fields", client_venue_path(@venue), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
